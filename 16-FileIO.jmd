# FileIO
filesystem zeug (readdir, rm, ...)
text dateien einlesen (DelimitedFiles, CSV)

Data serialization: CSV, Excel, FortranFiles, MAT, JLD, HDF5, JSON, BSON, XML

## Aufgabe: Daten einlesen 2 spaltig getrennt mir ; und , als dezimaltrennzeichen
