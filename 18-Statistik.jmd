# Statistik

In Julia sind einige grundlegende Statistikfunktionen im `Statistics` Paket enthalten.
Das Paket kann wie alle anderen Pakete mit `using` geladen werden.

```julia
using Statistics
```

- std
- var
- mean
- median
- Distributions paket
  - Distribution types
  - Create distrbution sample values plot
  - fit distribution to some data maybe with a random noise
- (cor)
- (cov)
- (quantile)
